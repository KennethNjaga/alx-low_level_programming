#include "main.h"
/**
 * rev_string -function that reverses a string
 * @s: character string
 *
 * Return: Nothing to return
 */
void rev_string(char *s)
{
int c;

for (c = 0; s[c] != '\0'; c++)

if (s[c] == '\0')

c--;
while
(c > 0);

}
